import { SplitDecimalPipe } from './split-decimal.pipe';

describe('SplitDecimalPipe', () => {
  it('create an instance', () => {
    const pipe = new SplitDecimalPipe();
    expect(pipe).toBeTruthy();
  });
});
