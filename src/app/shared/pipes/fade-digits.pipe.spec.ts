import { FadeDigitsPipe } from './fade-digits.pipe';

describe('FadeDigitsPipe', () => {
  it('create an instance', () => {
    const pipe = new FadeDigitsPipe();
    expect(pipe).toBeTruthy();
  });
});
